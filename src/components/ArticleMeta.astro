---
import { formatDate } from '../utils/format';

interface Props {
  entry: any;
  minutes: number;
}

const { entry, minutes } = Astro.props;
---

<div class="article-meta">
  <span title="Created">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2"/><path d="M16 2v4M8 2v4M3 10h18"/></svg>
    {formatDate(entry.data.date)}
  </span>
  {entry.data.updated && entry.data.updated.valueOf() !== entry.data.date.valueOf() && (
    <span title="Last updated">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12a9 9 0 1 1-3-6.7L21 8"/><path d="M21 3v5h-5"/></svg>
      Updated {formatDate(entry.data.updated)}
    </span>
  )}
  <span title="Estimated reading time">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
    {minutes} min read
  </span>
  {entry.data.category && entry.data.category !== 'uncategorized' && (
    <span title="Category">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20.59 13.41 13.42 20.58a2 2 0 0 1-2.83 0L2 12V2h10l8.59 8.59a2 2 0 0 1 0 2.82z"/><path d="M7 7h.01"/></svg>
      {entry.data.category}
    </span>
  )}
</div>

{entry.data.tags && entry.data.tags.length > 0 && (
  <div class="tag-list" style="margin-top: var(--sp-3);">
    {entry.data.tags.map((t: string) => <a class="tag" href={`/tag/${t}`}>#{t}</a>)}
  </div>
)}
